import * as counterActions from "./counter";
import * as bugTrackerActions from "./bugTracker";

console.log(counterActions);
console.log(bugTrackerActions);

export { counterActions, bugTrackerActions };
